This is a demo project for meetup application. User can create new meetups and set reminders or set as a favorite. 
Note that this project still in early development stages, so all the featuers might not be available.
Wait for stable version as this is only demo version. 